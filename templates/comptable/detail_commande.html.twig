<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Détail Commande #{{ commande.id }} - Moonlight</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ asset('utils/css_moon/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('utils/css_moon/fontAwesome.css') }}">
    <link rel="stylesheet" href="{{ asset('utils/css_moon/templatemo-main.css') }}">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22></text></svg>">
    <style>
        /* === FONDATIONS ET VARIABLES DE STYLE === */
        :root {
            --primary-color: #4a90e2;
            --success-color: #2ecc71;
            --warning-color: #f39c12;
            --danger-color: #e74c3c;
            --bg-color: #1a1c2d; /* Fond légèrement différent pour la page détail */
            --surface-color: rgba(255, 255, 255, 0.05);
            --border-color: rgba(255, 255, 255, 0.1);
            --text-primary: #ffffff;
            --text-secondary: rgba(255, 255, 255, 0.7);
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-primary);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            padding: 40px;
        }

        /* === EN-TÊTE DE LA PAGE === */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
            flex-wrap: wrap;
            gap: 20px;
        }

        .page-header h1 {
            font-weight: 300;
            font-size: 2.5em;
            margin: 0;
        }
        
        .page-header h1 span {
            font-weight: 600;
            color: var(--primary-color);
        }

        .btn-back {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background-color: var(--surface-color);
            color: var(--text-secondary);
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            border: 1px solid var(--border-color);
            transition: all 0.2s ease;
        }
        .btn-back:hover {
            background-color: var(--primary-color);
            color: var(--text-primary);
            border-color: var(--primary-color);
        }

        /* === GRILLE PRINCIPALE POUR LES CARTES === */
        .detail-grid {
            display: grid;
            grid-template-columns: 1fr 2fr; /* 1/3 pour les infos, 2/3 pour les paiements */
            gap: 30px;
        }
        
        /* Pour les écrans plus petits, les cartes se superposent */
        @media (max-width: 992px) {
            .detail-grid {
                grid-template-columns: 1fr;
            }
        }

        /* === STYLE GÉNÉRAL DES CARTES === */
        .detail-card {
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            backdrop-filter: blur(10px);
            overflow: hidden; /* Pour que les coins arrondis s'appliquent partout */
        }

        .detail-card-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 18px 25px;
            background: rgba(0, 0, 0, 0.15);
            border-bottom: 1px solid var(--border-color);
            font-size: 1.1em;
            font-weight: 500;
        }
        .detail-card-header i {
            color: var(--primary-color);
        }

        .detail-card-body {
            padding: 25px;
        }

        /* === CARTE D'INFORMATIONS (GAUCHE) === */
        .info-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .info-list li {
            display: flex;
            align-items: flex-start;
            gap: 15px;
            margin-bottom: 20px;
            font-size: 1.1em;
        }
        .info-list li i {
            font-size: 1.2em;
            color: var(--text-secondary);
            margin-top: 3px;
            width: 20px; /* Alignement parfait */
            text-align: center;
        }
        .info-list li span {
            font-weight: 500;
        }
        
        .due-date-warning {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: var(--danger-color);
            font-weight: 600;
        }

        /* === CARTE DE PAIEMENTS (DROITE) === */
        .payment-table {
            width: 100%;
            border-collapse: collapse;
        }
        .payment-table th, .payment-table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }
        .payment-table th {
            font-size: 0.8em;
            color: var(--text-secondary);
            text-transform: uppercase;
        }
        .payment-table tbody tr:hover {
            background-color: rgba(74, 144, 226, 0.1);
        }
        .payment-table tfoot {
            font-weight: 600;
            font-size: 1.1em;
        }
        .payment-table tfoot td {
            background: rgba(0,0,0,0.2);
        }
        .payment-table .text-end { text-align: right; }
        .payment-table .text-center { text-align: center; }

        /* Bouton d'action (imprimer) */
        .btn-action {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            width: 34px;
            height: 34px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            border: none;
            transition: all 0.2s ease;
        }
        .btn-action:hover {
            background: var(--primary-color);
        }
    </style>
</head>
<body>
    <div class="detail-page-wrapper">

        <!-- En-tête de la page -->
        <header class="page-header">
            <h1>Détail Commande <span>n°{{ commande.id }}</span></h1>
            <a href="{{ path('app_comptable_dashboard') }}" class="btn-back">
                <i class="fas fa-arrow-left"></i>
                <span>Retour au tableau de bord</span>
            </a>
        </header>

        <!-- Grille principale -->
        <div class="detail-grid">

            <!-- Colonne 1 : Informations sur la commande -->
            <div class="detail-card">
                <div class="detail-card-header">
                    <i class="fas fa-file-invoice"></i>
                    Informations Générales
                </div>
                <div class="detail-card-body">
                    <ul class="info-list">
                        <li>
                            <i class="fas fa-user"></i>
                            <div>
                                Client<br>
                                <span>{{ commande.client.nom }}</span>
                            </div>
                        </li>
                        <li>
                            <i class="fas fa-calendar-alt"></i>
                            <div>
                                Date de commande<br>
                                <span>{{ commande.dateCommande|date('d/m/Y') }}</span>
                            </div>
                        </li>
                        <li>
                            <i class="fas fa-credit-card"></i>
                            <div>
                                Mode de paiement<br>
                                <span>{{ commande.methodePaiement ?? 'Non spécifié' }}</span>
                            </div>
                        </li>
                        {% if commande.dateEcheance %}
                        <li>
                            <i class="fas fa-hourglass-half"></i>
                            <div>
                                Date d'échéance<br>
                                {% if commande.dateEcheance < "now" and commande.resteAPayer > 0 %}
                                    <span class="due-date-warning" title="Échéance dépassée !">
                                        <i class="fas fa-exclamation-triangle"></i>
                                        {{ commande.dateEcheance|date('d/m/Y') }}
                                    </span>
                                {% else %}
                                    <span>{{ commande.dateEcheance|date('d/m/Y') }}</span>
                                {% endif %}
                            </div>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </div>

            <!-- Colonne 2 : Liste des paiements -->
            <div class="detail-card">
                <div class="detail-card-header">
                    <i class="fas fa-history"></i>
                    Historique des Paiements
                </div>
                <div class="detail-card-body" style="padding: 0;">
                    <table class="payment-table">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Moyen</th>
                                <th class="text-end">Montant</th>
                                <th class="text-center">Reçu</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% set totalPaye = 0 %}
                        {% for paiement in commande.paiements %}
                            {% if paiement.statut == 'effectué' %}
                                {% set totalPaye = totalPaye + paiement.montant %}
                            {% endif %}
                            <tr>
                                <td>{{ paiement.datePaiement|date('d/m/Y H:i') }}</td>
                                <td>
                                    {{ paiement.referencePaiement }} 
                                    {% if paiement.statut != 'effectué' %}
                                        <span class="badge rounded-pill text-bg-warning">{{ paiement.statut }}</span>
                                    {% endif %}
                                </td>
                                <td class="text-end">{{ paiement.montant|number_format(0, ',', ' ') }} MGA</td>
                                <td class="text-center">
                                    {% if paiement.statut == 'effectué' %}
                                        <a href="{{ path('app_comptable_paiement_recu', {id: paiement.id}) }}" class="btn-action" title="Imprimer le reçu" target="_blank">
                                            <i class="fas fa-print"></i>
                                        </a>
                                    {% endif %}
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4" class="text-center" style="padding: 40px;">Aucun paiement enregistré pour cette commande.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan="2">Total Payé (Effectué)</td>
                                <td class="text-end">{{ totalPaye|number_format(0, ',', ' ') }} MGA</td>
                                <td></td>
                            </tr>
                            <tr style="background: rgba(0,0,0,0.4);">
                                <td colspan="2">TOTAL COMMANDE</td>
                                <td class="text-end">{{ commande.getTotalAvecFrais()|number_format(0, ',', ' ') }} MGA</td>
                                <td></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>

        </div>
    </div>
    
    <!-- Assurez-vous d'avoir Font Awesome chargé pour les icônes fas fa-... -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/js/all.min.js"></script>
</body>
</html>