<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Fiche de Production - Commande #{{ commande.id }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            color: #333;
        }
        .container {
            width: 100%;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 30px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
        }
        .info-grid {
            margin-bottom: 30px;
        }
        .info-grid td {
            padding: 5px;
            vertical-align: top;
        }
        .info-grid .label {
            font-weight: bold;
            width: 120px;
        }
        .products-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .products-table th, .products-table td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: left;
        }
        .products-table th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .checkbox-col {
            width: 50px;
            text-align: center;
        }
        .checkbox {
            width: 20px;
            height: 20px;
            border: 1px solid #333;
            display: inline-block;
        }
        .notes {
            margin-top: 40px;
            border: 1px solid #ccc;
            padding: 10px;
            min-height: 80px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Fiche de Production</h1>
        </div>

        <table class="info-grid">
            <tr>
                <td class="label">Commande N° :</td>
                <td><strong>#{{ commande.id }}</strong></td>
            </tr>
            <tr>
                <td class="label">Date :</td>
                <td>{{ commande.dateCommande|date('d/m/Y H:i') }}</td>
            </tr>
            <tr>
                <td class="label">Client :</td>
                <td>{{ commande.client.nom|default('N/A') }}</td>
            </tr>
        </table>

        <h2>Produits à Préparer</h2>
        <table class="products-table">
            <thead>
                <tr>
                    <th class="checkbox-col">Fait</th>
                    <th>Quantité</th>
                    <th>Produit</th>
                </tr>
            </thead>
            <tbody>
                {% for item in commande.commandeProduits %}
                    <tr>
                        <td class="checkbox-col"><div class="checkbox"></div></td>
                        <td><strong>{{ item.quantite }}</strong></td>
                        <td>{{ item.produit.nom }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="notes">
            <strong>Notes :</strong>
        </div>
    </div>
</body>
</html>