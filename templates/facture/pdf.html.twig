<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture {{ facture.id }}</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        table, th, td { border: 1px solid black; }
        th, td { padding: 8px; text-align: left; }
        h1 { text-align: center; }
        .total { text-align: right; font-weight: bold; }
    </style>
</head>
<body>
    <h1>Facture #{{ facture.id }}</h1>
    <p><strong>Client :</strong> {{ facture.client.nom }}</p>
    <p><strong>Date :</strong> {{ facture.dateCreation|date('d/m/Y') }}</p>
    <p><strong>Livreur :</strong> {{ facture.livreur }}</p>

    <table>
        <thead>
            <tr>
                <th>Produit</th>
                <th>Quantité</th>
                <th>Prix unitaire (MGA)</th>
                <th>Total (MGA)</th>
            </tr>
        </thead>
        <tbody>
            {% for ligne in facture.lignes %}
                <tr>
                    <td>{{ ligne.produit.nom }}</td>
                    <td>{{ ligne.quantite }}</td>
                    <td>{{ ligne.prixUnitaire|number_format(0, ',', ' ') }}</td>
                    <td>{{ ligne.prixTotal|number_format(0, ',', ' ') }}</td>
                </tr>
            {% endfor %}
            <tr>
                <td colspan="3" class="total">Frais de livraison</td>
                <td>{{ facture.fraisLivraison|number_format(0, ',', ' ') }}</td>
            </tr>
            <tr>
                <td colspan="3" class="total">Total</td>
                <td>{{ facture.total|number_format(0, ',', ' ') }}</td>
            </tr>
        </tbody>
    </table>
</body>
</html>
